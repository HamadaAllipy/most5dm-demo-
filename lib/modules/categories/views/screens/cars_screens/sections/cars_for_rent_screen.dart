import 'package:flutter/material.dart';
import 'package:most5dm/modules/categories/views/widgets/default_category_screen.dart';

class CarsForSellScreen extends StatelessWidget {
  const CarsForSellScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('CarsForSellScreen'),
      ),
    );
  }
}
