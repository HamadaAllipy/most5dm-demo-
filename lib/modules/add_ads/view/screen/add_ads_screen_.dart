import 'package:flutter/material.dart';

class AddAdsScreen_ extends StatelessWidget {
  const AddAdsScreen_({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple,
    );
  }
}
